{
  "name": "@nova/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main/main.js",
  "scripts": {
    "build": "pnpm --filter @nova/shared build && pnpm --filter @nova/memory prisma:generate && pnpm --filter @nova/memory build && pnpm --filter @nova/runtime build && vite build && tsc -p tsconfig.json && node scripts/copy-migrations.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "vite --host 127.0.0.1"
  },
  "dependencies": {
    "@nova/memory": "workspace:*",
    "@nova/runtime": "workspace:*",
    "@nova/shared": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^34.0.0",
    "vite": "^6.0.0"
  }
}
