# Funnels

Key funnels tracked: onboarding completion, first successful autonomous task, plugin install-to-first-use. Funnel steps are defined by event sequence, not by screen view, so the same funnel is measurable across chat/voice/CLI.
