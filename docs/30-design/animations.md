# Animations

Durations: 120ms micro-interactions, 200ms transitions, 300ms max for full-screen transitions. All animation respects `prefers-reduced-motion` by substituting an instant state change, never by skipping the state change entirely.
