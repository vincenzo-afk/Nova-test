# Adaptive Layout

Layout breakpoints are defined once in design tokens and consumed identically by desktop and the Android companion's responsive web views where applicable, so a layout bug fixed once doesn't need re-fixing per platform.
