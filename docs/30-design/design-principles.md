# Design Principles

1. Calm by default — no motion, color, or sound without informational purpose.
2. Every state is designed, not defaulted (`design-qa` enforces this).
3. Density is user-controlled; power users can compress the UI.
4. Trust is earned visually: destructive/irreversible actions always look different from reversible ones.
