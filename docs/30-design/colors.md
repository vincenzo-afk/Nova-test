# Colors

Semantic color roles (surface, border, text-primary, text-secondary, accent, success, warning, danger) map to different literal values in light/dark mode; components reference roles, never literal colors, so `dark-mode.md` requires zero component-level changes.
