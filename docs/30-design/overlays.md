# Overlays

The system-wide overlay (quick-capture, quick-ask) is a separate lightweight window from the main desktop app, launchable via global hotkey, sharing design tokens but not full app chrome — see `docs/09-ui/overlay.md`.
