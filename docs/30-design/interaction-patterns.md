# Interaction Patterns

Standard patterns: inline confirm for reversible actions, modal confirm for destructive ones, toast for passive notification, banner for degraded-mode/offline state. No screen invents a new pattern without adding it here first.
