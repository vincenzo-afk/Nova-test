# Loading States

Skeleton loaders for content with a known shape (lists, cards); indeterminate spinners only for genuinely unknown-duration operations (a Planner run). Every loading state has a documented maximum expected duration; beyond it, the UI switches to a 'taking longer than usual' state, never an infinite spinner.
