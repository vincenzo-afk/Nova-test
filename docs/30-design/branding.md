# Branding

Primary mark is the NOVA sparkle/orbit logo (`docs/assets/nova-logo.png`); minimum clear space equal to the mark's own height on all sides; never stretched, recolored outside the defined gradient, or placed on a background below the contrast minimum in `accessibility.md`.
