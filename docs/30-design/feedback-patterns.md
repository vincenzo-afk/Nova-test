# Feedback Patterns

Every user action produces feedback within 100ms (even if it's just a pressed-state change) — the perception of responsiveness is treated as a hard requirement, not a nice-to-have, given NOVA's actions often take longer to actually complete.
