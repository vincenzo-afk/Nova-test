# Partial Recovery

Recovery of a single corrupted subsystem (e.g. one memory tier) without a full-instance restore — isolates the damaged component, rebuilds it from source-of-truth where derivable, restores from backup where not.
