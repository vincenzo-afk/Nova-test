# Provider Selection Flow

## Flow

User adds a provider credential → NOVA validates it live → provider appears in routing pool with its capability tags shown → user can pin/exclude it per capability domain in `docs/18-providers/provider-routing.md` terms.

## Reference

See matching screen specs in `40-screens/` and component specs in `41-components/`.
