# Authentication Flow

## Flow

Local-only mode requires no authentication; cloud-sync mode uses passphrase-derived encryption key, never transmitted, with recovery-phrase generation shown once at setup and never retrievable again afterward (documented clearly to the user).

## Reference

See matching screen specs in `40-screens/` and component specs in `41-components/`.
