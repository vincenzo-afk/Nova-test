# Tool Execution Flow

## Flow

Planner selects a tool → permission check → (if required) user approval prompt shown with exact action preview → execution → Verifier check → result surfaced with a link back to the originating request.

## Reference

See matching screen specs in `40-screens/` and component specs in `41-components/`.
