# Workspace Flow

## Flow

Multi-workspace users switch context via the sidebar; each workspace has isolated memory/permissions unless explicitly shared, preventing the 'user has multiple workspaces with conflicting settings' failure case.

## Reference

See matching screen specs in `40-screens/` and component specs in `41-components/`.
