# Notifications

Three tiers: silent (logged only), passive (visible in-app, no OS notification), and active (OS notification). Only permission-requiring or decision-requiring events may use active tier, per `product-principles.md` rule 1. Every notification links directly to the memory/task that generated it.
