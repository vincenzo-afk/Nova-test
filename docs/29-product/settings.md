# Settings

Settings are organized by the same taxonomy as `18-providers/`, `docs/10-security/permissions.md`, and `16-extensibility/` so there is a 1:1 mapping between a settings screen and an architecture doc — no orphan settings that don't correspond to a documented, enforced behavior.
