# GPU Usage

Local model inference GPU usage is profiled per model size tier; hardware-detection (`docs/18-providers/hardware-detection.md`) re-checks availability rather than assuming a static snapshot from startup.
