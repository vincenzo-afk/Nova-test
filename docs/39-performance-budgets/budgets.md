# Performance Budgets

Chat first-token latency: <800ms local, <2s cloud fallback. Memory query: <150ms p95. App cold start: <2s to interactive. Any regression beyond budget blocks release per `docs/12-testing/benchmarks.md`.
