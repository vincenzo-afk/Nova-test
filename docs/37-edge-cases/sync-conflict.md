# Sync Conflict — Edge Case

## Scenario

N-way conflicts (not just two-device) must be explicitly tested, not assumed to reduce to the two-device case.

## Requirement

Every edge case in this directory must have an explicit test in `12-testing/` — an edge case with no test is an edge case that will regress silently.
