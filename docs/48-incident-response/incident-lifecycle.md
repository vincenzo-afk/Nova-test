# Incident Lifecycle

Detect → Triage (assign severity) → Mitigate (stop the bleeding) → Resolve (root cause fixed) → Postmortem. Every stage has an owner and a timestamp recorded, even for solo-maintainer incidents, to keep the historical record usable.
