# Root Cause Analysis

Five-whys minimum; the analysis is not complete until it reaches either a code defect, a missing test, or a missing doc — 'it just happened' is never an accepted root cause. Every confirmed root cause gets added to the relevant `45-code-perfection-failure-modes/` file if it's a novel pattern.
