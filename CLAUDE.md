# CLAUDE.md

> **This is a pointer.** The canonical agent-entrypoint rules live in
> [`AGENTS.md`](AGENTS.md) at the repository root. It is kept here only
> because some tools look specifically for `CLAUDE.md` — read
> `AGENTS.md`, not a duplicate copy, so there is exactly one place these
> rules can drift out of date. If you are Claude (or any other agent)
> reading this file, go read `AGENTS.md` in full before writing any
> code, and `docs/00-implementation-governance/` after that.
